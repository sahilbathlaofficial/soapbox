<%# by default local variable named as activity %>
<% if !(activity.trackable.nil?) %>
  <%= image_tag activity.owner.avatar, class: 'thumbnailImage'  %>
  You were tagged in 
  <%= link_to('post', activity.trackable, class: "notificationModalShow", data: {toggle: "modal"}, role: "button") %> 
<% end %>